# Third-party package versions (packages we don't control)
# Our packages (simple-slack-mcp-server, @mcp-tunnel/wrapper, @bugzy-ai/*) don't need pinning
PLAYWRIGHT_VERSION=1.50.1
PLAYWRIGHT_MCP_VERSION=0.0.62
NOTION_MCP_VERSION=2.1.0
